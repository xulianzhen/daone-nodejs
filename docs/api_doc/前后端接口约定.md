1: 登录注册(微信一键登录, 短信)
(1.1) 获取微信登录二维码
请求方式: GET
请求参数: 无
返回参数: qrUrl:string, expireTime:long (无手机号)

(1.2) 短信验证码
请求方式: POST
请求参数: phone:string, scene:string (1:登录, 2:忘记密码)
返回参数: boolean: 

(1,3)手机验证码登录
请求方式: POST
请求参数: phone:string, code:string
返回参数: token:string

(1.3) 轮询微信二维码扫描成功
请求方式: GET
请求参数: qrUrl:string
返回参数: boolean: true:成功, false:失败

2: 个人中心模块
(2.1) 获取个人信息
请求方式: GET
请求参数: 无
返回参数: userInfo:{
    userId: string, 
    userName: string,  // canEdit
    avatar: string,  // canEdit
    phone: string,
    email: string,  // canEdit
    gender: string,  // canEdit
    birthday: string,  // canEdit
    address: string,  // canEdit
    wechat: string,
    subscribe: {
        planCode: string,
        planName: string,
        expireTime: string,
        autoRenewal: boolean,  // 是否自动续费
    },
    integral: number,
    credit: {
        balance: number,
        monthlyGrant: number,
        isExpiredDays: number,
    }
}

(2.2) 修改个人信息
请求方式: POST
请求参数: userInfo:object
返回参数: boolean: true:成功, false:失败

(2.2) 退出登录
logout
请求方式: POST
请求参数: 无
返回参数: boolean: true:成功, false:失败

(2.3) 上传文件(接入风控)
请求方式: POST
请求参数: file:file scene:string
返回参数: fileInfo: {
    id: string,
    url: string,
    fileName: string,
    fileSize: number,
    fileType: string,
}

(2.4)作品列表 (搜索,分页)
请求方式: GET
请求参数: keyword:string, pageNumber:number, pageSize:number
返回参数: list: [{
    id: string,
    title: string,
    cover: string,
    createTime: string,
    updateTime: string,
}]

(2,5)画布的保存(新增/编辑)
请求方式: POST
请求参数: canvasData: JSON字符串
version: string id (非必填)
返回参数: version: string

(2.6)画布详情
请求方式: GET i
请求参数: id: string, version: string(version:非必填 不传的话取最新的版本数据)
返回参数: 
canvasData: JSON字符串
id: string,
version: string,
createTime: string,
updateTime: string,
versionList: [{
    version: string,
    createTime: string,
    updateTime: string,
}]

(2.7)字典接口 
请求方式: GET
请求参数: type: string

(2.8)画布删除
请求方式: DELETE
请求参数: id: string
返回参数: boolean: true:成功, false:失败

(2.9) 套餐列表
请求方式: GET
请求参数: 无
返回参数: list: [{
    id: string,
    name: string,
    price: number,
    description: Array<string>,
    integral: number,
    code: string,
    createTime: string,
    updateTime: string,
}];

(3.0) 订阅套餐
请求方式: POST
请求参数: planCode: string, cycle: string (1:月, 2:年) autoRenewal: boolean (是否自动续费)
返回参数: boolean: true:成功, false:失败  orderNo: string

(3.1) 支付
请求方式: POST
请求参数: orderNo: string payType: string (1:微信, 2:支付宝)
返回参数: boolean: true:成功, false:失败

(3.2) 轮询订单支付状态
请求方式: GET
请求参数: orderNo: string
返回参数: status: string (1:待支付, 2:支付成功, 3:支付失败)

(3.3) 订单列表记录
请求方式: GET
请求参数: pageNumber: number, pageSize: number
返回参数: list: [{
    id: string,
    orderNo: string,
    amount: number,
    createTime: string,
    updateTime: string,
}]

(3.4) 获取总积分
请求方式: GET
请求参数: 无
返回参数: totalIntegral: number

(3.5) 积分明细
请求方式: GET
请求参数: pageNumber: number, pageSize: number
返回参数: list: [{
    id: string,
    amount: srting,
    createTime: string,
    updateTime: string,
    user: srting;
}]
(3.6) 灵感发现
请求方式: GET
请求参数: pageNumber: number, pageSize: number, keyword: string
返回参数: list: [{
    id: string,
    title: string,
    cover: string,
    createTime: string,
    updateTime: string,
}]

(3.7) 取消自动续费
请求方式: POST
